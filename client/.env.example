# API 주소 (로컬 vs 배포)
# ─────────────────────────────────────────────────────────────
# 로컬 개발: 비우거나 아래처럼 두면 API + Tesla OAuth 모두 localhost:3000 사용
#   VITE_BASE_URL=
#   또는 VITE_BASE_URL=http://localhost:3000
#
# 배포( Coolify 등 ): .env 는 올리지 말고, 배포 플랫폼의 환경 변수에서 설정
#   VITE_BASE_URL=https://api.mongoori.com
# (빌드 시점에 들어가므로 배포 후 재빌드 필요)
# ─────────────────────────────────────────────────────────────
VITE_BASE_URL=

# 통화 (선택)
VITE_CURRENCY=USD
